# Finds a duplicate in a list of numbers (with arbitrary number of duplicates) in O(n) time, using linked list.

class ListNode(self,value,nextNode):
    self.value = value
    self.next = nextNode
    self.visited = False

